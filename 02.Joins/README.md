# Tema 02: JOINS

###  Lo que aprendí
* Obtener informacion de dos tablas diferentes
* Usar alias para cada tabla `AS`.
* Encontrar la "clave" que une a dos tablas
 

###  Lo que se me dificultó
* El orden de las tablas al usar `LEFT JOIN` y `RIGHT JOIN`
* Entender como interpretar `NULL`
* Saber que tabla conservar

### 🚀 Desafío superado
En ejercicio de `RIGHT JOIN` se solicito mostrar compras aunque el usuario no exista escribi la consulta como si estuviera usando `LEFT JOIN` (Ejercicio 13) 
 
