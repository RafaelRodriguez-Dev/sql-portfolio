# Tema 01: Select y Filtros

###  Lo que aprendí
* Modo o sintaxis de escritura SQL
* Cómo seleccionar columnas específicas con `SELECT`.
* Filtrado de datos usando `WHERE`, `AND` y `OR`.
* El uso de `LIKE` para buscar patrones de texto.
* Usar `LIMIT` para limitar los resultados 

###  Lo que se me dificultó
* Al principio me confundía escribiendo al usar dos filtros o usar dos condiciones. Mi error: `...WHERE  usuario_id = 2 OR 3`
* Entender el orden cuando queria ver resultados de mayor a menor usando `ORDER BY` y `ASC`, `DESC`
* Buscar escribir un codigo o consulta mas eficiente sin tener aun el conocimiento y practica adecuada

### 🚀 Desafío superado
Se solicito mostrar el top 2 de compras mas baratas, solucione limitando los resultados con `LIMIT`, "aun pienso que existe una mejor manera"
 
